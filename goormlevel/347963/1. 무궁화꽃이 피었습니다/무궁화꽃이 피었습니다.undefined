# 1. 세 정수 D (목표 거리), M (이동 거리), T (노래 시간)를 입력받습니다.
D, M, T = map(int, input().split())

# 2. 전체 걸리는 시간을 계산합니다.
# 시간 = 전체 거리 / (M / T) 이므로, 정리하면 (D * T) / M 이 됩니다.
total_time = (D * T) / M

# 3. 소수점 이하를 반올림하여 정수로 출력합니다.
print(round(total_time))